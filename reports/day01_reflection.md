The concept that clicked most immediately today was Python functions. I understood how to define a function, give it arguments, return a value, and use a docstring to explain its purpose. I also found basic Python data types and f-strings fairly straightforward.

The concept that is still a little fuzzy is NumPy broadcasting. I understand that NumPy can apply the column means across all rows without using a loop, but I am still not completely clear on how NumPy decides which dimensions can be combined automatically. I would like to practice this with more examples.

One question I want answered at the start of tomorrow's session is: how do I know when I should use NumPy arrays instead of normal Python lists for an AI or data-processing task?

The assignment took me roughly 4-5 hours, including writing the code, testing each requirement, fixing the file path for the chart, and checking that the notebook runs correctly.